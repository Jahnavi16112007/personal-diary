services:
  - type: web
    name: personal-diary-app
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python "jahnavi.personal diary.py"
    envVars:
      - key: FLASK_ENV
        value: production
